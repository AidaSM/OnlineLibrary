@model List<OnlineLibrary.Models.TransactionModel>

<!-- Your view content -->

<h1>My Transactions</h1>

<table>
    <thead>
        <tr>
            <th>Book Title</th>
            <th>Date</th>
            <th>Return Date</th>
            <th>Status</th>
            <!-- Add other headers as needed -->
        </tr>
    </thead>
    <tbody>
        @foreach (var transaction in Model)
        {
            <tr>
                <td>@transaction.BookTitle</td>
                <td>@transaction.Date</td>
                <td>@transaction.Retrun</td>
                <td>@transaction.Status</td>
                <!-- Add other cells as needed -->
            </tr>
        }
    </tbody>
</table>
